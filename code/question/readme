1. 一堆racer，每个racer有出发时间和到达时间，计算每个racer的score，规则如下 ：

score ＝ 所有出发比自己晚但是到达比自己早的racer数量之和，（所有的出发时间 和到达时间没有重复的）要求时间复杂度o(nlgn).

2. Determine whether an integer is a palindrome. Do this without extra space. O(1) is OK

3. Given a string S, find the longest palindromic substring in S.

4. Given a string s, partition s such that every substring of the partition is a palindrome.

5.Given a 2D board containing 'X' and 'O', capture all regions surrounded by 'X'.

6. Find the total sum of all root-to-leaf numbers.

7. Given an unsorted array of integers, find the length of the longest consecutive elements sequence.

8. Word Ladder

9. Validate Palindrome

10. Flatten a BST

11. subset of array


13, merge interval

14. Jump game.

15. N queens

17. Search in rotated sorted array

18. divide int without * or / in log(n)

19. Given n, print all result of "()()()"

20, Print string from phone pad

21. find the kth number from two sorted array

22. KMP String matching algorithm

23. Remove 'x', double 'z', assume # of x >= z

24. Given a string.Find the longest substring in it such that the substring contains only 2 unique characters.Ex- aabbcbbbadef Ans-bbcbbb
