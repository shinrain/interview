1. 一堆racer，每个racer有出发时间和到达时间，计算每个racer的score，规则如下 ：

score ＝ 所有出发比自己晚但是到达比自己早的racer数量之和，（所有的出发时间 和到达时间没有重复的）要求时间复杂度o(nlgn).

2. Determine whether an integer is a palindrome. Do this without extra space. O(1) is OK

3. Given a string S, find the longest palindromic substring in S.

4. Given a string s, partition s such that every substring of the partition is a palindrome.

5.Given a 2D board containing 'X' and 'O', capture all regions surrounded by 'X'.

